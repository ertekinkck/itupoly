name: itupoly_engine
description: "İTÜpoly oyun motoru — saf Dart, Flutter bağımsız, event-sourced, deterministik."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.1

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.0.0
